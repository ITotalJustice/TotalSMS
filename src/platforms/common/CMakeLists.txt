set(COMMON_SOURCES)
set(COMMON_INCLUDES ${CMAKE_CURRENT_SOURCE_DIR})

if (USE_ARGS)
    list(APPEND COMMON_SOURCES ${CMAKE_CURRENT_SOURCE_DIR}/args/args.c)
endif()

set(COMMON_SOURCES ${COMMON_SOURCES} PARENT_SCOPE)
set(COMMON_INCLUDES ${COMMON_INCLUDES} PARENT_SCOPE)
